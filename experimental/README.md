# Experimental compiler harness

This directory contains experimental compiler and fixture work. It has no stable API, compatibility guarantee, or complete language semantics.

Run all experimental checks locally:

```bash
go test ./...
```
